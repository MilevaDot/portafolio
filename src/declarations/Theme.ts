import { extendTheme } from "@chakra-ui/react";

const Theme = extendTheme({
    fonts: {
        heading: `'Winky Sans', sans-serif`,
        body: `'Winky Sans', sans-serif`,
    },
})

export default Theme